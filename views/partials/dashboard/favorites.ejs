<!-- Favorites Section -->
<section id="favorites" class="section hidden ml-[300px]">
    <div class="flex justify-between items-center mb-8">
        <div>
            <h2 class="text-3xl font-bold">Favorites</h2>
            <p class="text-gray-600 dark:text-gray-400">Your favorite artworks</p>
        </div>
        <button class="text-sm px-4 py-2 rounded-lg bg-[#1a1a1a] text-white hover:bg-[#2a2a2a] transition-all">
            <i class="fas fa-sync-alt mr-2"></i>Refresh
        </button>
    </div>

    <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6" id="favoritesGrid">
        <!-- Favorites will be loaded dynamically -->
    </div>
</section> 